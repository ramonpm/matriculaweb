<%= form_for(@usuario) do |f| %>
  <% if @usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
      <% @usuario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :curso_id %><br />
    <%= f.collection_select :curso_id, @cursos, :id, :nome, {:include_blank => 'Escolha um Curso'} %>
  </div>
  <div class="field">
    <%= f.label :matricula %><br />
    <%= f.number_field :matricula %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :administrador %><br />
    <%= f.check_box :administrador %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>